@model  System.Data.DataSet

@{
    ViewBag.Title = "ActualizarPais";
    ViewBag.BodyClass = "vista";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section scripts {
    <script src="~/JS/jsPais.js"></script>
}

<h2 class="contenedor table-title">Actualización de paises</h2>

<form class="contenedor form-principal" action="/Pais/Actualizar" method="post">
    <div>
        <div class="formulario card border-primary">
            <div class="card-header formulario-header">
                <label class="p-2">Actualizar Pais</label>
                <button onclick="location.href = '@Url.Action("Pais", "Pais")'; return false;" class="btn btn-success">Regresar</button>
            </div>

            <div class="col" align="center">
                <div class="form-group">
                    <label class="p-2">ID del País (3 letras)</label>
                    <input type="text" readonly value="@Model.Tables[0].Rows[0]["idPais"]" required class="form-control mb-2 input-readonly" id="idPais" name="idPais" />
                </div>
            </div>

            <div class="col" align="center">
                <div class="form-group">
                    <label class="p-2">Nombre del País</label>
                    <input type="text" value="@Model.Tables[0].Rows[0]["nombre"]" required class="form-control mb-2" id="nombre" name="nombre" />
                </div>
            </div>

            <div class="contenedor-btn">
                <button type="submit" class="btn btn-primary btn-guardar" onclick="validarGuardadoDeCampos(event);">Guardar</button>
            </div>

        </div>
    </div>
</form>





